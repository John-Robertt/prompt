# 高效工具选择与调用指南

本指南提供工具选择、调用顺序、结果复用和上下文控制建议。

## 1. 选择工具

识别当前信息缺口，调用能直接填补它的最小工具；结果足够后停止定位，不要再用其他工具重复同一工作。下表是效率建议，各工具的接口约束以对应小节为准。

| 当前需要 | 首选工具 |
|---|---|
| 理解未知实现、调用关系或影响范围 | `codegraph_explore`（仓库存在索引时） |
| 按文件名、目录或路径定位 | `fffind` |
| 按标识符、文本、错误信息或配置项定位 | `ffgrep` |
| 按语法结构或调用形态定位 | `ast-grep` |
| 查看已知文件或获取编辑锚点 | `read` |
| 局部修改已有文件 | `replace` |
| 撤销错误替换 | `undo_last_replace` |
| 创建文件或完整重写 | `write` |
| 执行命令 | `bash`（RTK 自动压缩） |
| 查询第三方当前技术文档 | Context7 |
| 隔离广泛调查或并行独立任务 | 子代理 |

常见组合路径：

```text
未知实现        → codegraph_explore → 信息不足时再使用定位工具或 read
已知路径        → read
文本或标识符定位 → ffgrep → 必要时 read
语法结构定位    → ast-grep → 必要时 read
局部编辑        → read → replace → 检查返回的 diff
新建或完整重写  → write → 检查实际文件内容或变更
```

## 2. 理解与定位

仓库存在 `.codegraph/`，且当前需要理解代码位置、实现机制、调用关系、影响范围或准备修改的符号时，优先使用 `codegraph_explore`。

- 查询应包含功能问题、关键符号或文件名，而不是宽泛关键词。
- 将返回的源码视为已经读取，不要立即重复调用 `read`。
- CodeGraph 已覆盖目标源码时，只有准备使用 hash 锚点编辑，或需要其未覆盖的精确局部，才再调用 `read`。
- 没有 `.codegraph/` 时，按当前信息缺口直接使用 `read` 或定位工具。

定位建议：
- 已知目标路径且只需查看内容时，直接使用 `read`。
- `fffind` 只用于路径，不用于文件内容。
- `ffgrep` 优先搜索裸标识符，并尽量限制目录和排除噪声。
- 搜索结果已满足当前信息需求时停止；需要查看目标内容时再读取。仍有歧义时，根据已有结果调整查询，不要无依据地重复或扩大搜索。
- 文本匹配容易误报时，通过 `bash` 使用完整命令 `ast-grep`；不要使用 `sg`。
- 常规搜索不要退回 Bash 的 `find`、`grep` 或 `rg`，除非上述工具无法表达需求。

## 3. 读取与修改文件

### 修改已有文件

遵循：

```text
read → replace → 检查返回的 diff → 下一次 replace
```

- `read` 只读取需要理解或修改的局部范围。
- `replace` 的范围必须尽可能小，只包含实际变化的行。
- 同一文件不要并行执行多个 `replace`。
- 每次修改后先检查 diff，再继续编辑。
- 锚点过期时重新 `read`，不要猜测新锚点。
- 如果替换误删或破坏了内容，立即调用 `undo_last_replace`。

### 创建或完整重写文件

- 新文件使用 `write`。
- 只有确实需要完整重写时才用 `write` 覆盖已有文件。
- 局部修改已有文件时不要用整文件覆盖代替 `replace`。
- 使用 `write` 后检查实际文件内容或最终变更，确认写入结果符合预期。

## 4. 执行命令与控制输出

通过 `bash` 直接执行正常命令。RTK 会透明压缩其支持的 Git、搜索、测试和构建输出，不要默认手工添加 `rtk` 前缀。

- 命令应尽量限定文件、目录、测试目标或输出范围。
- 需要运行验证命令时，先执行最小、最相关的目标，再按需要扩大范围。
- 需要诊断完整原始输出，或怀疑压缩隐藏关键信息时，使用：

```bash
RTK_DISABLED=1 <command>
```

- 处理 JSON 时优先用 `jq` 提取所需字段，不要读取或打印整个大型文件。
- 避免用 Bash 输出大段源码；源码读取交给 `read`。

## 5. 外部文档与网络信息

- 查询库、框架、SDK、API、CLI 或云服务的当前用法时，使用 Context7。
- 先解析库 ID，再按单一主题查询文档；不同主题分别查询。
- 一般外部事实、近期信息或 Context7 不覆盖的内容再使用网页搜索。
- 优先依据官方或第一方来源，区分已验证事实与推断。

## 6. 子代理

通常在以下情况考虑使用子代理：

- 搜索范围广且一次直接探索不足以回答。
- 多个相互独立的问题可以并行调查。
- 调查会产生大量中间信息，需要隔离上下文。
- 复杂跨模块任务需要独立实施计划。

已知文件、已知符号或单点修改直接使用当前会话工具。委派后不要在主会话重复同一搜索；子代理若修改文件，必须检查实际差异和验证结果。
