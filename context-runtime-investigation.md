# Context Runtime 技术调查方案

## 文档职责

本文定义一项技术调查：验证是否有必要、且是否能够由独立于任务模型的 Context Runtime，持续构造完成当前工程步骤所需的最小充分工作集。

本文是调查与决策方案，不是最终实现规范。未经对照实验验证的机制，不进入正式实现设计。

## 主要矛盾

长时程工程任务会持续产生用户要求、代码、工具结果、错误、测试和决策证据；任务模型在任一时刻能够可靠利用的注意力却是有限的。

因此，本调查要解决的主要矛盾是：

> 如何在未来相关性不完全可知的情况下，将持续增长的完整历史投影成一个有界、充分、可信且与当前步骤高度相关的工作集，同时保证任何被移出的事实仍可恢复。

这个矛盾包含两类对称风险：

- **遗漏风险**：关键目标、约束或证据未进入工作集，造成错误决策、重复调查和返工。
- **干扰风险**：已经失去当前价值或与事实相似但不正确的内容继续占用注意力，增加推理难度、延迟和成本。

Context Runtime 的核心责任不是压缩 token，而是控制这两类风险。压缩、摘要、检索、冷热分层、Epoch 和 Provider 缓存都是从属机制。

## 核心调查问题

调查按顺序回答三个问题：

1. 对目标模型和真实工程任务，聚焦的最小充分工作集是否稳定优于完整历史？
2. 如果优于，Runtime 能否自动构造接近人工 oracle 的工作集，而不把上下文管理变成任务模型的新任务？
3. 如果能够，所得质量收益是否大于归档、检索、Sidecar、缓存冷启动和错误恢复的总成本？

只有三个问题均得到肯定答案，完整 Context Runtime 才有实现价值。

## 已确认事实

### 1. Pi 会话会持续积累上下文

会话 `019ff033-4362-7b07-8158-b9ddb7072064` 的 74 个非零 usage assistant 调用显示：

- 首次物理输入上下文约 14.7K tokens。
- 最后一次约 245.5K tokens。
- 最大值约 245.5K tokens。
- 会话中没有发生 compaction。

这里的物理输入上下文定义为：

```text
physicalInput = input + cacheRead + cacheWrite
```

该会话已经接近 Pi 为 `openai-codex/gpt-5.6-sol` 配置的 272K 上限。历史增长不是待证明的假设。

### 2. 工具结果是主要增长来源

同一会话约包含 640K 文本字符，其中约 95.9% 来自工具结果。主要来源包括：

- `read`：约 350K 字符。
- `get_search_content`：约 126K 字符。
- `ffgrep`：约 51K 字符。
- `fetch_content`：约 45K 字符。
- assistant 文本：约 26K 字符。
- user 文本：不足 1K 字符。

字符数不等同于 token，也不表示这些工具结果都无关；它只说明最先值得验证的缩减位置是已完成阶段的大型工具证据，而不是用户要求。

### 3. Provider 缓存降低计费，但不减少模型面对的上下文

上述会话中，包含 usage 字段的 assistant 消息第 39–58 条构成固定快照（其中第 55 条 usage 为 0）：

- 普通输入：453,407 tokens。
- cache read：3,162,624 tokens。
- cache write：0 tokens。
- 物理输入：3,616,031 tokens。
- cache read 占物理输入约 87.46%。

按 GPT-5.6 的 0.1 cache-read 费率系数折算：

```text
billableInputEquivalent
  = input
  + 0.1 × cacheRead
  + 1.25 × cacheWrite
  = 769,669.4
```

相对全部按普通输入计费，理论折算减少约 78.72%。该数字用于架构比较；Codex 订阅的实际额度和服务端扣费不能由 usage 完整推导。

缓存已经显著缓解重复输入成本，但没有减少模型实际处理的 160K–245K 上下文。因此缓存优化不能替代工作集选择。

### 4. 长上下文影响取决于任务和模型

现有外部证据不能支持“上下文越长必然越差”的绝对结论：

- Lost in the Middle 发现相关信息位置变化会显著影响多文档问答和键值检索。
- Context Rot 在控制任务难度、只增加输入长度时，观察到多模型性能下降；聚焦输入在 LongMemEval 上优于包含大量无关历史的完整输入。
- Google 报告 Gemini 2.5 Flash 在简单事实检索中没有明显 Lost-in-the-Middle。
- OpenAI 报告 GPT-4.1 在简单 needle retrieval 上表现稳定，但多项关联和多跳推理仍然困难。

因此，主要矛盾对复杂、多证据、含相似干扰项的长时程任务具有充分调查价值，但其对 GPT-5.6 Sol 工程任务的实际贡献必须由本方案的阶段 0 直接验证。

外部依据：

- [Anthropic: Effective context engineering for AI agents](https://www.anthropic.com/engineering/effective-context-engineering-for-ai-agents)
- [Lost in the Middle](https://arxiv.org/abs/2307.03172)
- [Chroma: Context Rot](https://research.trychroma.com/context-rot)
- [Google: Retrieval Quality at Context Limit](https://research.google/pubs/retrieval-quality-at-context-limit/)
- [OpenAI: Introducing GPT-4.1](https://openai.com/index/gpt-4-1/)

## 待验证假设

### H1：聚焦工作集具有实际价值

在固定任务、模型和执行环境下，人工构造的最小充分工作集相对完整历史：

- 任务质量更高，或至少满足预先定义的非劣效界限；并且
- 输入、延迟或总执行成本至少一项获得预先定义的实质改善；并且
- 改善不是由更多返工、重复调查或工具调用换取。

### H2：确定性工具结果处理是最低风险的第一杠杆

对已经完成阶段的大型测试、构建、日志、搜索和 JSON 结果进行确定性精简，可以减少明显冗余，同时保持修复问题所需的关键证据。

### H3：Runtime 可以逼近 oracle

不向任务模型暴露上下文管理流程或工具时，Runtime 自动生成的工作集在必要证据召回、约束保持和任务完成质量上接近人工 oracle。

### H4：缓存优化只能在工作集正确后产生净收益

当 Runtime 已能稳定控制遗漏和干扰风险后，追加式 Epoch 和缓存感知 checkpoint 才能进一步降低成本；不能为了 cache-read ratio 保留已经证明会损害任务质量的内容。

## 竞争性解释

调查必须主动区分以下替代解释：

1. 目标模型能够可靠利用完整历史，工作集投影没有质量收益。
2. 当前主要瓶颈只是物理窗口或定价边界，确定性截断和原生 compaction 已经足够。
3. Runtime 无法观察任务模型下一步真正需要的信息，自动选择造成的遗漏成本高于完整历史的干扰成本。
4. 大部分收益来自工具输出精简，不需要 TaskState、语义检索或 Sidecar。
5. 成本主要由少数冷请求决定，Context Runtime 的复杂度无法改变最终结果。

## 成功标准

完整 Context Runtime 进入实现设计前，必须同时满足：

### 任务质量

- Runtime 相对完整历史满足预先登记的非劣效界限。
- 用户目标、验收条件和安全约束保持率不下降。
- 因遗漏历史导致的错误决策、重复调查和返工不增加。
- Runtime 与 oracle 的质量差距处于预先登记的可接受范围。

### 工作集质量

- 最新有效用户要求以原文保留。
- 当前任务所需的关键证据具有可测量的高召回率。
- 活动工具调用与结果保持协议完整，不拆散 assistant tool call 与对应结果。
- 工作集中的摘要事实均有来源引用，且原文可恢复。
- 无关或相似干扰内容相对完整历史显著减少。
- 投影失败、状态损坏或检索失败时可安全回退。

### 资源使用

- 总成本至少一项获得预先登记的实质改善：实际货币成本、首次正确完成时间或资源消耗。
- Sidecar、索引、缓存冷启动、恢复、额外工具调用和返工成本全部计入。
- 工作集构建时间不会随完整 session 长度不可接受地增长。
- Runtime 失败不阻断 Pi 原生上下文处理和任务执行。

“非劣效界限”“实质改善”和样本量不能在看到最终结果后决定。阶段 0 的 pilot 只用于估计方差；正式评估前冻结任务集、主要指标、界限和分析方法。

## 非目标

本调查不以以下事项为目标：

- 让单个会话无限增长。
- 追求最高压缩率或最低 token 数。
- 证明所有模型都会发生统一的 context rot。
- 让任务模型执行压缩、分层或缓存管理流程。
- 用摘要替代代码、测试结果、用户原始要求等权威事实。
- 在工作集价值尚未验证前替换或关闭 Pi 原生 compaction。
- 为所有 Provider 强行建立统一缓存状态机。

## 权威事实与模型视图

### L0：权威来源

L0 保存或引用：

- 完整 Pi session JSONL。
- 原始用户消息。
- Runtime 改写前、工具实际返回的精确 ToolResult。
- 可获得的未截断工具输出或权威输入快照。
- 工作集、摘要和状态使用的证据引用与校验信息。

L0 不常驻模型上下文，只用于恢复、检索和验证。

### L1：任务锚点

候选内容包括：

- 当前目标和验收条件。
- 当前仍有效的用户约束。
- 已确认的关键决策及理由。
- 当前阶段和阻塞。

任务锚点必须小、稳定、结构化，并引用 L0。未经证据确认的 Sidecar 推断不能写成已确认事实。

### L2：热工作集

按活动状态选择并优先保留原文：

- 最新用户要求。
- 当前错误、失败测试和直接相关日志。
- 当前分析或修改的文件、symbol 及直接依赖。
- 尚未完成的工具调用与结果。
- 当前步骤所需的精确 API、数据结构和代码证据。

热工作集不能仅按消息时间定义。

### L3：相关历史

在独立预算内按需选择：

- 已完成阶段的高保真摘要。
- 相关历史决策和约束。
- 精确错误字符串、路径、symbol 和 API 约定。
- 与当前依赖链相关的历史工具结果片段。

摘要和索引只负责定位，不是唯一事实来源。

## 工具原文边界

Pi 当前的通用 `tool_result` hook 发生在工具实现已经返回结果之后。某些内置工具在返回前已经截断内容，因此 Runtime 不能声称总能获得“截断前的原始执行输出”。

调查按三类处理：

1. **可直接归档**：工具提供完整结果或稳定的完整输出路径，例如发生截断时的部分 bash 结果。
2. **可保存权威快照**：Runtime 能根据工具输入在执行时保存权威来源快照，例如受支持的文件读取。
3. **只能保存实际 ToolResult**：自定义工具或不提供恢复来源的工具。

成功标准中的“原文可恢复”默认指 Runtime 改写前的精确 ToolResult。若产品要求所有工具的截断前执行输出均可恢复，必须先通过以下接口闸门之一：

- Pi Core 提供截断前的 raw-result hook；或
- Runtime 覆盖受支持工具并建立统一 raw-output contract。

在接口闸门通过前，不扩大该承诺。

## 候选任务状态

只有阶段 2 证明需要时，才调查以下最小结构：

```ts
interface TaskState {
  goal: string;
  acceptanceCriteria: string[];
  constraints: string[];
  currentPhase: string;
  completed: string[];
  inProgress: string[];
  decisions: Array<{
    decision: string;
    rationale: string;
    evidenceRefs: string[];
  }>;
  blockers: string[];
  activeFiles: string[];
  activeSymbols: string[];
  exactErrors: string[];
  nextActions: string[];
}
```

调查重点不是结构是否能生成，而是它是否会把临时推断、过期目标或错误归因固化为长期事实。

## 评估设计

### 任务集

选择可重复、具有确定验收方法的真实工程任务，至少覆盖：

- 多文件缺陷定位与修复。
- 长日志或大型工具输出分析。
- 跨阶段重构。
- 包含明确用户约束的实现任务。
- 需要恢复较早决策或证据的长时程任务。
- 具有相似错误、相似 symbol 或冲突历史的干扰任务。

每次运行从同一代码提交、配置和数据状态开始，并隔离文件修改和缓存条件。

### 固定条件

正式对照中固定：

- 模型和版本。
- thinking level、最大输出和 service tier。
- Pi、扩展和工具版本。
- system prompt、工具集合和工具 schema。
- 初始仓库提交及依赖状态。
- transport 和 Provider 配置。

冷缓存与热缓存分层报告，不混合为单一均值。

### 对照组

对每项任务至少构造：

| 组别 | 上下文策略 | 目的 |
|---|---|---|
| F | Pi 完整历史/默认行为 | 当前基线 |
| O | 人工或事后标注的 oracle 最小充分工作集 | 估计工作集选择的理论收益上限 |
| D | 仅确定性工具结果精简 | 判断低风险局部能力是否已经足够 |
| R | Runtime 自动任务锚点与热工作集 | 评估自动选择能力 |
| RR | R + 自动历史检索 | 评估检索对遗漏与噪声的净影响 |
| RC | 质量通过后的 RR + 缓存感知 Epoch | 评估缓存优化的增量价值 |

执行顺序随机化；根据 pilot 方差确定重复次数和正式样本量。

### Oracle 构造

Oracle 不是任意摘要。它由完成任务所需的最小证据集合组成，包括：

- 有效用户要求和验收条件。
- 产生正确修改所需的代码和接口。
- 定位根因所需的错误、测试和日志。
- 当前仍约束方案的历史决策。

Oracle 的每一项内容均引用 L0。任务完成后由成功轨迹、测试证据和独立复核共同标注，避免仅由同一任务模型自评。

### 主要指标

#### 任务质量

- 任务完成率。
- 验收条件满足率。
- 用户约束保持率。
- 首次正确完成时间。
- 错误修改和返工次数。
- 根因重复调查次数。
- 因缺失或干扰历史导致的错误决策次数。

#### 工作集质量

- 必要证据召回率。
- 工作集证据精确率。
- 无关及相似干扰 token 比例。
- 摘要事实错误率。
- 原文恢复成功率。
- 工具协议完整率。
- Runtime 与 oracle 的 token、证据和任务质量差距。
- 发生遗漏后的恢复时间、工具调用和成本。

#### 资源使用

- `input`、`cacheRead`、`cacheWrite`、`output`。
- 实际货币成本或 Provider 可验证的额度消耗。
- 冷请求次数及其成本占比。
- Provider prompt-cache 命中和稳定前缀存活轮次。
- WebSocket delta continuation 使用率。
- checkpoint 冷启动成本和实际回本轮次。
- Sidecar、索引、归档和投影成本。
- session 与索引磁盘增长。
- 投影构建时间随 session 长度的变化。

## 调查阶段与决策闸门

### 阶段 0：验证主要矛盾

对同一任务比较 F、O，并增加带相似干扰项的完整历史变体。

验证：

- 聚焦工作集是否改善复杂、多证据任务的质量或稳定性。
- 完整历史中的无关内容是否产生可重复的干扰。
- 收益是否只来自 token/延迟，还是也体现在正确性和返工。
- 不同上下文长度、相关信息位置和干扰类型的影响。

交付物：

- 冻结的任务集和运行方法。
- oracle 证据标注。
- 完整历史、oracle 和干扰变体的质量—长度曲线。
- 正式评估的非劣效界限、实质改善阈值和样本量。

决策闸门：

- O 不优于 F：停止完整 Runtime，转向局部工具输出精简和成本优化。
- O 优于 F：主要矛盾在目标工作负载上成立，进入阶段 1。

### 阶段 1：原文边界与确定性工具结果处理

实现最小归档原型和少量高价值处理器，不改变历史消息投影。

优先覆盖：

1. 测试输出。
2. 构建日志。
3. 搜索结果。
4. 大型 JSON。
5. 可恢复完整输出的 bash 结果。

必须保留：

| 内容类型 | 精简视图 | 必须保留 |
|---|---|---|
| 测试 | 失败用例、堆栈和统计 | 测试名、错误原文、文件与行号 |
| 构建 | 失败阶段和关键诊断 | 退出码、目标名称、错误原文 |
| 日志 | 去重、分类和相关尾部 | 时间、级别、关键上下文 |
| Git | 状态和相关 diff hunk | 路径、修改类型、精确 hunk |
| 搜索 | 命中路径、symbol 和局部上下文 | 查询、路径、匹配文本 |
| JSON | 相关字段投影 | 字段路径、类型、精确值 |
| 源码 | symbol-aware excerpt | 路径、symbol、范围和内容 |

交付物：工具结果处理原型、恢复报告和证据保真度报告。

决策闸门：

- D 已获得大部分 oracle 收益：保留局部能力，不增加 TaskState 和语义检索。
- D 仍与 O 有明显差距：进入阶段 2。

### 阶段 2：任务锚点与热工作集

实现最小 TaskState、活动文件/symbol 跟踪和确定性工作集选择，暂不启用语义检索或 Sidecar 写入确认事实。

验证：

- 用户目标和验收条件是否稳定保留。
- 已完成内容能否安全退出热区。
- 当前错误、活动代码和未完成工具链是否不会被提前移除。
- R 相对 O 的遗漏与噪声差距。
- 状态损坏时能否从 L0 重建或回退。

交付物：Context Projector 原型、状态一致性测试和 R/O 差距报告。

决策闸门：

- R 无法满足质量非劣效：停止自动投影，保留阶段 1。
- R 质量通过但因历史缺失仍有明显差距：进入阶段 3。

### 阶段 3：自动历史检索

加入精确标识符、路径、symbol、错误字符串和关键词检索；只有这些方法不足时才实验语义重排。

每次检索查询可由以下确定性信号组成：

```text
最新用户消息
+ 当前任务阶段
+ 活动文件与 symbol
+ 当前错误字符串
+ 未完成事项
```

候选内容依次考虑：

1. 精确路径、symbol、错误字符串和标识符匹配。
2. 与当前依赖链的关系。
3. 来源权威性。
4. 语义相关性。
5. 最近实际使用时间。
6. 去重与信息多样性。

验证：

- 必要历史是否在需要时自动出现。
- 相似但错误的历史是否被排除。
- 检索增加的召回是否大于新增噪声。
- 检索失败是否可观察、可回退。

交付物：检索评估集、RR/O 差距、召回率、精确率和恢复成本报告。

### 阶段 4：可选 Sidecar

只有确定性状态更新或检索重排仍不能满足阶段 3 闸门时，才加入隔离 Sidecar。

Sidecar 可生成：

- 已完成阶段总结候选。
- TaskState 更新候选。
- 检索候选重排。
- 工作集充分性告警。

Sidecar 必须：

- 使用隔离会话。
- 输出结构化结果和证据引用。
- 无权删除 L0。
- 无权把无来源推断写成已确认事实。
- 失败时不阻断任务模型。
- 计入总成本和延迟。

事件 handler 默认会被等待；如需非阻塞更新，必须使用有序后台队列，并定义状态版本、并发、取消、session 切换和关闭语义。

交付物：Sidecar 增量收益、错误事实率、延迟和成本报告。

### 阶段 5：缓存感知的 Epoch

只有 R 或 RR 已满足质量闸门后，才调查 Epoch。Epoch 服务于正确工作集，而不是反过来。

Epoch 内保持：

```text
稳定系统提示词
稳定工具定义
稳定 checkpoint
epoch 内追加式历史
---------------- 缓存稳定前缀
本轮动态背景
最新消息与工具结果
```

规则：

1. Epoch 内已有消息只追加，不删除、不重排、不改写。
2. 阶段完成后仍无价值的内容只能在计划性 checkpoint 时退出。
3. 动态工作集采用有限 sticky residency，不能为了缓存永久保留。
4. 工具定义和 system prompt 变化视为潜在缓存重置。
5. 质量、协议完整性和定价边界是硬约束；cache-read ratio 不是。
6. checkpoint 或投影失败时返回 Pi 原生上下文。

必须分别实验：

- OpenAI Codex WebSocket 与 SSE。
- direct OpenAI implicit 与 explicit cache mode。
- Anthropic 5 分钟与 1 小时 TTL。
- 临时 `context` 投影与持久化 custom message。
- 不同压缩率、任务剩余长度和冷/热缓存条件。

交付物：Provider 分离的缓存状态机、质量约束下的 reset 阈值和实际成本曲线。

### 阶段 6：端到端正式评估

比较 F、O、D、R、RR、RC。

只有 RC 在任务质量非劣效、工作集可信且总成本改善的前提下，完整方案才进入实现规范。

## 缓存成本模型

缓存模型只用于阶段 5 的次级决策。

设：

- `P`：旧缓存前缀长度。
- `S`：新 checkpoint 前缀长度。
- `r`：cache read 相对费率。
- `w`：cache write 相对费率。
- `C`：生成 checkpoint、索引和冷启动的等价成本。
- `N`：预计新 Epoch 剩余模型请求数。

近似成本：

```text
keepCost(N) = N × r × P

resetCost(N) = w × S + (N - 1) × r × S + C
```

仅当以下条件成立时，成本模型支持重置：

```text
N > ((w - r) × S + C) / (r × (P - S))
```

该公式假设 TTL 有效、后续前缀稳定、费率可验证，且没有遗漏质量损失。正式决策还必须加入：

- 工作集质量硬约束。
- TTL 失效概率。
- 动态尾部和检索成本。
- 输出、Sidecar 与恢复成本。
- 长上下文价格层级。
- Codex 订阅无法从 usage 验证的实际扣费差异。

## Provider 边界

### OpenAI GPT-5.6+

- 缓存要求一致的 prompt prefix。
- cache read 约为普通输入价的 0.1 倍。
- cache write 约为普通输入价的 1.25 倍。
- direct API 支持 implicit/explicit cache mode；Pi 当前没有通过类型化消息接口自动标记稳定 breakpoint。
- `prompt_cache_key` 改善相同前缀的路由，但不放宽前缀一致性要求。
- GPT-5.6 超过 272K input 后进入更高价格层；cache-read token 同样属于输入规模。

### OpenAI Codex

- Pi 使用稳定 session ID 作为 cache key。
- WebSocket delta continuation 要求请求体除 `input` 外一致，并要求当前 input 精确继承上次请求与响应。
- 历史裁剪、重排、内容替换或工具/schema/system prompt 变化可能退出 continuation。
- `before_provider_request` 看到的是 transport 转换前的完整 payload，不能仅凭它判断是否实际发送 delta。
- Pi AI 当前提供进程内 Codex WebSocket debug stats，但这属于 Provider adapter 能力，不是通用 ExtensionContext 指标。

### Anthropic

- 缓存顺序为 `tools → system → messages`。
- 5 分钟写入约为普通输入价的 1.25 倍，1 小时写入约为 2 倍，读取约为 0.1 倍。
- 工具、系统提示词或 breakpoint 前消息变化会影响缓存。
- Runtime 应按会话间隔实验 TTL，不能默认使用 1 小时缓存。

外部依据：

- [OpenAI Prompt Caching](https://developers.openai.com/api/docs/guides/prompt-caching)
- [Anthropic Prompt Caching](https://platform.claude.com/docs/en/build-with-claude/prompt-caching)

## Pi 接入与边界

### `tool_result`

- 归档 Runtime 改写前的实际 ToolResult。
- 对支持类型生成确定性精简视图。
- 保持 `details`、`isError`、`usage` 和工具协议结构。
- 不声称通用获得截断前的执行输出。

### `message_end` / `turn_end`

- 收集状态候选、关键词、标识符和活动证据。
- 同步路径只执行确定性、低延迟处理。
- Sidecar 必须进入有序后台队列。

### `before_agent_start`

- 根据最新用户消息和已确认状态完成一次检索。
- Pi 当前先追加用户消息，再追加该 hook 返回的持久化 custom message；因此持久化检索消息位于最新用户消息之后。
- 若实验要求检索内容位于用户消息之前，只能由 `context` 投影插入，并明确它不是持久化 session 顺序。
- 同一用户轮次的后续 tool loop 不重复检索。

### `context`

- 每次模型调用前获得消息副本，可返回投影后的 messages。
- 必须保持用户要求、活动工具协议和当前证据完整。
- Epoch 内默认只监测和追加；计划性 checkpoint 才重建工作集。
- 自身错误必须返回接收到的 `event.messages`；不能依赖异常后继续使用部分投影。

### `session_start`

- 恢复 Runtime 状态和索引。
- 验证状态引用与 session JSONL 一致。
- 状态损坏时从 L0 重建或禁用 Runtime。

### `session_before_compact`

- 调查阶段不关闭 Pi 原生 compaction。
- Runtime checkpoint 是否复用 `CompactionEntry`，必须在阶段 2 与自有投影方案比较后决定。
- 原生 compaction 保留为溢出恢复路径。

### 用户可见接口

只提供 `/context-status` 一类用户命令，展示：

- 当前工作集及分区占用。
- 本轮检索来源和证据引用。
- 归档与状态一致性。
- 投影、回退、缓存和 continuation 状态。

该接口不注册为模型工具，不占用任务模型提示词。

## 风险与证伪条件

出现以下任一情况，应停止扩大实现并回到最近的决策闸门：

- O 相对 F 没有稳定质量或资源收益。
- R/RR 相对 F 的任务成功率或约束保持率下降。
- 模型因缺失历史重复调查或作出错误修改。
- 相似历史被错误检索并覆盖当前事实。
- Sidecar 错误事实进入已确认任务锚点。
- 工作集构建时间随 session 长度不可接受地增长。
- 动态投影破坏缓存前缀，成本超过精简收益。
- 高 cache-read ratio 与更低任务质量同时出现。
- checkpoint 未在任务结束前回本。
- Provider 不支持预期的 breakpoint、TTL 或 continuation 语义。
- Runtime 故障阻断任务模型，而不是安全回退。
- 为保证自动管理而增加的隐式复杂度高于直接使用 Pi 原生 compaction 和工具精简。

## 最终决策矩阵

| 实验结果 | 决策 |
|---|---|
| O 不优于 F | 主要矛盾不是当前首要约束；停止完整 Runtime |
| O 优于 F，D 已接近 O | 只保留原文边界与确定性工具精简 |
| O 优于 F，R 明显差于 F | 问题成立，但自动投影路线失败 |
| R 通过质量闸门，RR 无增量收益 | 不加入自动历史检索 |
| RR 接近 O，但成本更高 | 保留研究结果，不进入正式实现 |
| RR 接近 O，且改善总成本 | 进入缓存感知 Epoch 调查 |
| RC 质量非劣效且总成本改善 | 编写正式实现规范 |

## 最小推荐路线

```text
F 与 O 的反事实对照
→ 原文边界验证
→ 确定性工具结果精简
→ 最小任务锚点与热工作集
→ 精确历史检索
→ 必要时 Sidecar
→ 质量通过后的缓存感知 Epoch
→ 端到端正式评估
```

在 R 或 RR 通过质量闸门前，不加入模型可见的上下文管理工具、消息引用协议、压缩提醒，也不以缓存命中率作为项目成功指标。

## 最终决策问题

调查结束后只回答：

> 对目标模型和真实工程任务，Context Runtime 能否在不降低任务质量、不增加任务模型上下文管理负担的前提下，稳定构造接近 oracle 的最小充分工作集，并降低任务完成的实际总成本？

答案为“是”时，根据通过验证的最小机制编写实现规范；答案为“否”时，只保留已证明有效的局部能力，不继续增加上下文管理复杂度。
